from distutils.core import setup
setup(
  name = 'bloodyterminal',
  packages = ['bloodyterminal'], # this must be the same as the name above
  version = '0.1',
  description = 'A little helper to structure your terminal output',
  author = 'TheBloodyScreen',
  author_email = 'jaydee@thebloodyscreen.com',
  url = 'https://github.com/peterldowns/mypackage', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['terminal', 'console', 'print', 'debug'], # arbitrary keywords
  classifiers = [],
)